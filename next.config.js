/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                protocol: 'https',
                hostname: 'vjepnlfarrwxlpduhxxy.supabase.co',
            },
        ],
    },
}

module.exports = nextConfig
